import React from "react";
import ReactMarkdown from 'react-markdown'; // 使用 `react-markdown`
import { geminiGenerate } from "../hook/gemini";

const Home = () => {
  React.useEffect(() => {

  }, []);

  return (
    <div>
    </div>
  )
}

export default Home;